<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:padding="12dp">

    <ImageView
        tool:src="@mipmap/ic_launcher"
        android:id="@+id/artistSmall"
        android:layout_width="60dp"
        android:layout_height="60dp"
        />

    <TextView
        tool:text="Artist name"
        android:id="@+id/artistNameIndividual"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toRightOf="@id/artistSmall"
        app:layout_constraintBottom_toBottomOf="@id/artistSmall"
        app:layout_constraintTop_toTopOf="@id/artistSmall"
        android:layout_marginLeft="16dp"
        android:textSize="20sp"
        android:lines="1"
        android:ellipsize="end"
        />

</androidx.constraintlayout.widget.ConstraintLayout>